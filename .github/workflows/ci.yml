name: Build and Deploy Next.js Docker Image

on:
  push:
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub # Using Access Token
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Create .env file
        run: |
          echo 'IMAGE_HOSTNAME=${{ secrets.IMAGE_HOSTNAME }}' >> .env.production.sample 
          echo 'NEXT_PUBLIC_API_SYMFONY_URL=${{ secrets.SYMFONY_URL }}' >> .env.production.sample 
          echo 'NEXT_PUBLIC_URL=${{ secrets.NEXT_URL }}' >> .env.production.sample 
          echo 'NEXTAUTH_URL=${{ secrets.NEXT_URL }}' >> .env.production.sample 
          echo 'NEXTAUTH_SECRET=${{ secrets.NEXTAUTH_SECRET }}' >> .env.production.sample 
          echo 'GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_ID }}' >> .env.production.sample 
          echo 'GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_SECRET }}' >> .env.production.sample 
          echo 'KEY_SYMFONY_API=${{ secrets.KEY_SYMFONY_API_PROD }}' >> .env.production.sample
          echo 'NEXT_PUBLIC_MERCURE_URL=${{ secrets.MERCURE_URL }}' >> .env.production.sample

      - name: Build Docker image
        run: docker compose -f docker-compose-build.yml build nextjs

      - name: Push Docker image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/go_ludo_front:latest

      - name: Deploy to production server
        if: success() && github.ref == 'refs/heads/master'
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_KEY_PASSPHRASE }}
          script: |
            cd /var/www/go_ludo_front
            docker compose down
            rm docker-compose.yml
            docker rmi ${{ secrets.DOCKER_USERNAME }}/go_ludo_front:latest || true
            echo "${{ secrets.DOCKER_COMPOSE_FILE }}" >> docker-compose.yml
            docker pull ${{ secrets.DOCKER_USERNAME }}/go_ludo_front:latest
            docker compose up -d
